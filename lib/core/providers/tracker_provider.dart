import 'package:flutter/material.dart';

/// Central state provider for Tracking status and general tracker module coordinates.
class TrackerProvider extends ChangeNotifier {
  TrackerProvider() {
    debugPrint('TrackerProvider initialized');
  }
}
